# Archived

Issue tracking moved to GitHub Issues on 2026-08-24. Every open item was
migrated — priority as `p0`…`p4` labels, dependencies as GitHub's native
sub-issue and `blocked-by` links — and the workflow is now
`scripts/issue-claim.sh` and the `/issue` skill. See `CLAUDE.md`.

Nothing reads this directory. It is kept only so the reasoning recorded in
these records is not lost; `bd` is not a dependency of this project and does
not need to be installed to work on it.
