[package]
name = "postio-ui"
description = "Toolkit-free presentation logic shared by every Postio frontend (ADR 0019)"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
publish.workspace = true
repository.workspace = true

[lints]
workspace = true

[dependencies]
base64 = "0.22"
thiserror = "2.0.20"
postio-body = { version = "0.1.0", path = "../postio-body" }
postio-core = { version = "0.1.0", path = "../postio-core" }
postio-model = { version = "0.1.0", path = "../postio-model" }
postio-search = { version = "0.1.0", path = "../postio-search" }

[dev-dependencies]
postio-config = { version = "0.1.0", path = "../postio-config" }
