//! `postio-tui`: Postio in the terminal (`specs/005-tui-frontend`).

fn main() -> std::process::ExitCode {
    eprintln!("postio-tui: not built yet (specs/005-tui-frontend T039)");
    std::process::ExitCode::FAILURE
}
