//! `postio-daemon`: the store's one owner, serving frontends over a socket.

fn main() -> std::process::ExitCode {
    eprintln!("postio-daemon: not built yet (specs/005-tui-frontend T021)");
    std::process::ExitCode::FAILURE
}
